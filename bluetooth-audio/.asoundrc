pcm.!default {
	type plug
	slave.pcm rate96000Hz
}

pcm.rate96000Hz {
	type rate
	slave {
		pcm writeFile # Direct to the plugin which will write to a file
		format S16_LE
		rate 96000
	}
}

pcm.writeFile {
	type file
	slave.pcm null
	file "/var/cache/snapcast/snapfifo"
	format "raw"
}